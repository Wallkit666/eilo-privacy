import styled from 'styled-components';

import AboutExpertsCard from './card'

import { responsiveSize } from "@utils/responsive";

export const Container = styled.div`

`

export const Card = styled(AboutExpertsCard)`

`

// ${responsiveSize(1000)}